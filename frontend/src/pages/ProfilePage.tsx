export function ProfilePage() {
  return (
    <div className="placeholder-card" style={{ border: "1px dashed var(--border)", borderRadius: "var(--radius-lg)", padding: "1.5rem" }}>
      <h2>My Profile</h2>
      <p style={{ color: "var(--text-muted)" }}>
        The Me / Info / Projects / CVs tabs, autosave, and optimistic locking are coming in Phase 3.
      </p>
    </div>
  );
}
